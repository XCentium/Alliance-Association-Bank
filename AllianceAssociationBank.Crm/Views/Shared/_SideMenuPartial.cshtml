<nav class="navbar p-1">
    @*p-0*@
    @*pl-3*@
    <button class="btn-img" id="nav-side-menu-toggle" type="button" data-toggle="slide-collapse-right" data-target="#nav-side-menu" aria-controls="nav-side-menu" aria-expanded="false" aria-label="Side Menu">
        <img class="icon-image" src="~/Content/Images/toggle-img.png" />
    </button>

    <div class="popup-control" id="nav-side-menu">

        <div class="pb-3 text-center">
            @Html.RoleBasedActionLink("Create New Project", ProjectsControllerAction.Create, ControllerName.Projects, null, "btn btn-primary btn-sm")
        </div>

        <div class="pb-3">
            <div class="nav-item px-3 py-1 border">
                @Html.ActionLink("Home", HomeControllerAction.Index, ControllerName.Home, routeValues: null, htmlAttributes: null)
            </div>
        </div>

        <div class="pb-3">
            <span class="text-muted">Reports</span>
            <div class="nav-item px-3 py-1 border border-bottom-0">
                @Html.RouteLink("Boarding", routeName: ReportsControllerRoute.ViewReport, routeValues: new
                {
                    name = ReportName.Boarding
                })
            </div>
            <div class="nav-item px-3 py-1 border border-bottom-0">
                @Html.RouteLink("Completed & Hold", routeName: ReportsControllerRoute.ViewReport, routeValues: new
                {
                    name = ReportName.CompletedAndHold
                })
            </div>
            <div class="nav-item px-3 py-1 border border-bottom-0">
                @Html.RouteLink("Software Transition", routeName: ReportsControllerRoute.ViewReport, routeValues: new
                {
                    name = ReportName.SoftwareTransition
                })
            </div>
            <div class="nav-item px-3 py-1 border border-bottom-0">
                <a href="#">CMC by ID</a>
            </div>
            <div class="nav-item px-3 py-1 border border-bottom-0">
                <a href="#">CMC by Name</a>
            </div>
            <div class="nav-item px-3 py-1 border border-bottom-0">
                <a href="#">CD Emails</a>
            </div>
            <div class="nav-item px-3 py-1 border">
                <a href="#">Coupon</a>
            </div>
        </div>

        <div class="pb-3">
            <span class="text-muted">Exports</span>
            <div class="nav-item px-3 py-1 border  border-bottom-0">
                @Html.RouteLink("CMC List", routeName: ExportsControllerRoute.GenerateExportFile, routeValues: new
                {
                    name = ExportName.CmcList
                })
            </div>
            <div class="nav-item px-3 py-1 border  border-bottom-0">
                @Html.RouteLink("CMC Useful Info List", routeName: ExportsControllerRoute.GenerateExportFile, routeValues: new
                {
                    name = ExportName.CmcUsefulInfoList
                })
            </div>
            <div class="nav-item px-3 py-1 border">
                <a href="#">All Info</a>
            </div>
        </div>

        <div class="nav-item text-right">
            @using (Html.BeginForm(UserControllerAction.Logout, ControllerName.User, FormMethod.Post, new
            {
                id = "side-menu-logout-form"
            }))
            {
                @Html.AntiForgeryToken()
                <a href="javascript:document.getElementById('side-menu-logout-form').submit()">Logout</a>
            }
        </div>

    </div>

</nav>
